---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: myds

spec:
  selector:
    matchLabels:
      env: dev

  template:
    metadata:
      labels:
        env: dev

    spec:
      containers:
        - name: cont-2
          image: nginx
          ports:
            - containerPort: 80
...
